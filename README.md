# В командной строке в папке проекта
git init
git add .
git commit -m "First commit"
git remote add origin https://github.com/ваш_username/clash-rule-builder.git
git branch -M main
git push -u origin main
